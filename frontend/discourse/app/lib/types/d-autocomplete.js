/**
 * @typedef AutocompleteResultsSignature
 *
 * @property {object} Args
 * @property {Array} Args.results - Array of autocomplete results
 * @property {number} Args.selectedIndex - Currently selected index in the results list
 * @property {function} Args.onSelect - Callback function triggered when a result is selected (result, index, event) => void
 * @property {function} [Args.onRender] - Optional callback function triggered after component renders (results) => void
 */

// Empty export to make this a module for JSDoc imports
export {};
