/**
 * Strict inequality helper
 * @param {unknown} left - First value to compare
 * @param {unknown} right - Second value to compare
 * @returns {boolean} Whether values are not strictly equal
 */
export default function notEq(left, right) {
  return left !== right;
}
