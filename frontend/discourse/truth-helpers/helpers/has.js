/**
 * Check if a Set has an item
 * @param {Set<unknown>} set - Set to check
 * @param {unknown} item - Item to check for
 * @returns {boolean} Whether set has the item
 */
export default function has(set, item) {
  return set.has(item);
}
