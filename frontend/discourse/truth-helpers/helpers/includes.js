/**
 * Check if an array includes an item
 * @param {unknown[]} array - Array to check
 * @param {unknown} item - Item to check for
 * @returns {boolean} Whether array includes the item
 */
export default function includes(array, item) {
  return array.includes(item);
}
