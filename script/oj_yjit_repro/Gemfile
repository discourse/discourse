source "https://rubygems.org"

# CI overrides via OJ_VERSION; defaults to the suspect build for local runs.
gem "oj", ENV.fetch("OJ_VERSION", "3.16.13")

# Required by Oj.optimize_rails — pin to the version Discourse runs.
gem "activesupport", "8.0.5"
