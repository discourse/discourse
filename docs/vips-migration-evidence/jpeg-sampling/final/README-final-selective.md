Final selective source snapshot: 2ba8357019543403265a8b4cd24c906815484efe

All current runtime source and harness checksums are verified before execution. Previous scripts/manifests are preserved under history/pre-selective; historical results and outputs remain in their original locations. New image outputs go to outputs-final-selective and results to final-selective.json. Do not reuse historical result files for resume.

Run: `RESULT_PATH=final-selective.json bundle exec ruby run.rb`.

The sampling companion has80cases. JPEGconversion andorientation each include two unchanged valid uncommon-sampling fixtures. Orientation uses identity orientation for those two cases. The full current worker is included with its complete local helper dependency set.
