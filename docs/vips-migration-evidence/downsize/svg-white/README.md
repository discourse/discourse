This directory contains the c63 SVG-only rerun. Its raw results and exact source bundle are separate from the parent 2ba full-corpus report. Only matching SVG rows supersede the parent report in sample-map.json. Encoded outputs match their recorded SHA-256 values. Fresh-worker hashes equal the corresponding native warm output; separate cold files are retained only where retrieved files still match the recorded hash.
