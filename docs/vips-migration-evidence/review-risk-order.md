Review the smallest contracts first, then the operations that can replace uploaded pixels or affect the widest set of thumbnails. The ordering below is a review-risk judgment, not a claim that earlier operations cannot fail or that recorded tests establish universal compatibility. It uses the existing [remaining-callsite audit](remaining-callsite-audit.md), [exact branch manifest](review-stack-manifest.json), final operation evidence and the narrow caller paths already identified in this task.

| Review order, least → most risk | Prepared branch | Operation | Main reason |
| --- | --- | --- | --- |
| 1 | 01 (`345c05377b20`) | Animation probe | UploadCreator’s fallback after FastImage cannot classify animation. Returns a boolean and writes no image, but a wrong result can change conversion or animation handling. Small frame-count contract and paired fixtures make it the narrowest review. |
| 2 | 02 (`60ee5446c33c`) | SVG dimensions | Upload creation and Upload dimension reads. No raster output is adopted, but wrong units, viewBox fallback or zero-size handling can reject uploads or distort layout. XML parsing and worker boot need review; less codec/metadata surface than rendering. |
| 3 | 03 (`3f82276023c2`) | SVG asset rendering | SVG avatar/logo assets used by topic OG generation. Narrow generated-asset path and recoverable output. Relative assets, font access, background and scaling differences matter; measured SVG rendering is not pixel-identical. |
| 4 | 05 (`4c106c0a96e7`) | HEIF conversion | Upload ingress for a limited source family. Replaces the uploaded representation, so bit depth, alpha, embedded transforms and profiles matter. Explicit native decoder plus seven measured cases limits the surface, though it is riskier than derived SVG assets. |
| 5 | 06 (`a29087e98045`) | ICO conversion | Favicon-oriented upload conversion has narrow fanout, but the custom directory/bitmap decoding path adds a larger parsing surface: last-entry selection, PNG/BMP payloads, masks and bit depths. Malformed-input handling and RGB555 acceptance differences deserve more scrutiny than the limited caller count suggests. |
| 6 | 04 (`9c2219b3bec3`) | Full topic OG rendering | Derived share cards are recoverable, but the renderer combines fonts, text fitting, title/category layout, palettes and relative SVG assets. Missing Arabic/CJK glyphs in the pinned production image remain an evidence limitation. More visual/product risk than the adjacent conversion PRs, despite its earlier stack position. |
| 7 | 08 (`7548d192b0fc`) | JPEG auto-orientation | Nonidentity JPEG uploads can have their source pixels replaced. All eight transforms, EXIF reset, baseline/progressive behavior, ICC and temporary adoption are consequential; scope remains JPEG-only. Thirteen final cases and shared sampling evidence cover the measured worker, not complete caller/optimizer behavior. |
| 8 | 07 (`0a96aa3f57a2`) | JPEG conversion | Broader upload ingress than orientation: PNG/JPEG/GIF/WebP/AVIF, first-frame rules, white alpha flattening, CMYK, ICC and savings-based adoption. The GIF ICC regression and source-sampling corrections demonstrate real compatibility traps. Uncommon sources become larger 444 outputs; many small cases are slower. |
| 9 | 11 (`938adc3edff7`) | Crop | The crop branch of OptimizedImage.create_for feeds cooked-post and chat thumbnails. North gravity, fill sizing, cropping, sharpening, output codecs, SVG white background and post-optimizer color behavior combine. Derived thumbnails are replaceable, but incorrect content removal is directly visible. |
| 10 | 10 (`61227f084ac0`) | Resize | Default OptimizedImage.create_for path reaches ordinary thumbnails, cooked posts, chat and additional avatar/upload resize callers. It shares crop’s codec/profile/optimizer surface with broader default fanout and extent/interpolation behavior. The unused colors option was explicitly removed; unsupported quantization is not a remaining migration requirement. |
| 11 | 09 (`fab2dead9640`) | Downsize | UploadCreator’s size-reduction loops and ShrinkUploadedImage use percentage, area and bounding-box geometry, including same-path replacement. A rounding, quality or repeated-reduction error can alter an accepted upload itself, not only a regenerable thumbnail. Broad formats, source preservation, SVG flattening and optimizer/color handling make this the highest-risk prepared transform. |
| 12 | [PR43478](https://github.com/discourse/discourse/pull/43478) | Image quality | The query writes no image, but its estimates drive recompression, orientation and geometry output encoding. JPEG quantization estimates, non-JPEG defaults, frame concatenation and first-frame encoding carry-over span the widest set of callers. The compatible estimator and required attribution are approved; 26 representative queries and a 500-case JPEG matrix match the production helper. |

Practical merge order remains the prepared linear stack: **01 animation → 02 SVG dimensions → 03 SVG assets → 04 OG → 05 HEIF → 06 ICO → 07 JPEG → 08 orientation → 09 downsize → 10 resize → 11 crop → 12 image quality**. Each branch is based on its predecessor. SVG assets/OG reuse SVG infrastructure; JPEG introduces shared source-quality/sampling support used later; geometry operations share loading/saving helpers. Reviewing orientation before JPEG, or crop/resize before downsize, means examining those branch deltas with their dependencies present, not merging them first. Reordering merges would require an intentional restack and renewed source alignment/verification.

This is why ICO’s narrow favicon path still ranks above HEIF’s native decoder, OG ranks above its stack neighbors, and downsize ranks above the wider thumbnail resize path: implementation surface and irreversible pixel replacement matter alongside caller count. Thumbnail crop/resize failures usually affect derived outputs; upload downsize, orientation and conversion can determine the retained original representation. Temporary-file adoption reduces partial-write risk but does not prevent a successful-looking visual regression.

All prepared operations use the existing default-off feature flag; disabling it retains the legacy processing path. There is no claim of transparent ImageMagick fallback for native failures. Existing optimizers remain a separate stage: raw ICC retention does not prove post-optimizer color or metadata parity, and the optimizer evidence owner’s final findings should be applied before publication. Shared JPEG sampling and SVG background evidence cover known cross-operation changes, including deliberate uncommon 422/440→444 encoding and filtered-edge differences.

The final integration audit found no remaining enabled-path ImageMagick image-processing call; the quality probes are now native. The ImageMagick problem check still tests executable availability and is not a transform; it remains outside this processing migration. Letter-avatar/dominant-color work predates these twelve operation deltas and is outside this ordering. Exact review-head tests, publication state and CI are separate from risk ranking; this document does not report or infer their status.
