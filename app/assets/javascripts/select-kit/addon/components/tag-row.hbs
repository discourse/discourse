{{#if this.isTag}}
  {{discourse-tag
    this.rowValue
    noHref=true
    count=this.item.count
    tagGroups=this.item.tag_group_names
  }}
{{else}}
  <span class="name">{{this.item.name}}</span>
{{/if}}