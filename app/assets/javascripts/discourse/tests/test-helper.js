/**
 * 👋 Greetings Discourse Developer. This file is unused but is needed to keep
 * the Embroider/Webpack build happy. In the classic build, test-helpers.js
 * (plural!) is synthesized in ember-cli-build.js. In Embroider, it expects
 * this file (singular!) to be here but is incorporated (`require()`-ed) in
 * the test bundle unconditionally.
 */
