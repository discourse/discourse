<DateTimeInput @date={{from}} @onChange={{action "onChangeRanges" (hash prop="from")}} @showTime={{showFromTime}} @class="from" @placeholder={{i18n "dates.from_placeholder"}} />

<DateTimeInput @date={{to}} @relativeDate={{from}} @onChange={{action "onChangeRanges" (hash prop="to")}} @timeFirst={{toTimeFirst}} @showTime={{showToTime}} @clearable={{clearable}} @class="to" @placeholder={{i18n "dates.to_placeholder"}} />
