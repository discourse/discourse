{{#if value}}
  <DButton @class="btn-default group-manage-logs-filter" @action={{action "clearFilter"}} @actionParam={{type}} @icon="times-circle" @translatedLabel={{concat label ": " filterText}} />
{{/if}}
