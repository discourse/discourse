{{#if post}}
  <div class="reviewable-conversation-post">
    {{#if showUsername}}
      <LinkTo @route="user" @model={{post.user}} class="username">@{{post.user.username}}</LinkTo>
    {{/if}}
    {{html-safe post.excerpt}}
  </div>
{{/if}}
