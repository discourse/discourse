<TextField @value={{composer.title}} @id="reply-title" @maxLength={{titleMaxLength}} @placeholderKey={{composer.titlePlaceholder}} @aria-label={{I18n composer.titlePlaceholder}} @disabled={{disabled}} @autocomplete="off" />

<PopupInputTip @validation={{validation}} />
