{{#if onlyHex}}<span class="add-on">#</span>{{/if}}<TextField @class="hex-input" @value={{hexValue}} @maxlength={{maxlength}} @input={{action "onHexInput" value="target.value"}} />
<input class="picker" type="color" value={{normalizedHexValue}} {{on "input" this.onPickerInput}}>
