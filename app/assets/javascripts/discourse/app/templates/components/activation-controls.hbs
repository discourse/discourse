{{#unless siteSettings.must_approve_users}}
  <DButton @action={{sendActivationEmail}} @label="login.resend_title" @icon="envelope" @class="btn-primary resend" />
{{/unless}}

{{#if canEditEmail}}
  <DButton @action={{editActivationEmail}} @label="login.change_email" @icon="pencil-alt" @class="edit-email" />
{{/if}}
