<EmailGroupUserChooser @id="private-message-users" @value={{splitRecipients}} @onChange={{action "updateRecipients"}} @options={{hash
    topicId=topicId
    none="composer.users_placeholder"
    includeMessageableGroups=true
    allowEmails=currentUser.can_send_private_email_messages
    autoWrap=true
  }} />
