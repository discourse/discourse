{{#if @prefixValue}}
  <span class={{concat-class "sidebar-section-link-prefix" @prefixType @prefixCSSClass}} style={{@prefixCSS}}>
    {{#if (eq @prefixType "image")}}
      <img src={{@prefixValue}} class="sidebar-section-link-prefix-image">
    {{/if}}

    {{#if (eq @prefixType "text")}}
      <span class="sidebar-section-link-prefix-text">
        {{@prefixValue}}
      </span>
    {{/if}}

    {{#if (eq @prefixType "icon")}}
      {{d-icon @prefixValue class="sidebar-section-link-prefix-icon"}}
    {{/if}}

    {{#if @prefixBadge}}
      {{d-icon @prefixBadge class="sidebar-section-link-prefix-badge"}}
    {{/if}}
  </span>
{{/if}}
