<a href="{{badgeUrl}}">
  {{#badge-button badge=badge}}
    {{#if showGrantCount}}
      <span class="count">
        (×
        {{count}}
        )
      </span>
    {{/if}}
  {{/badge-button}}
</a>