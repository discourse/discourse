{{#if isEditing}}
  {{d-icon "pencil"}}
{{else}}
  {{composer-actions
    composerModel=model
    options=model.replyOptions
    whispering=model.whisper
    canWhisper=canWhisper
    action=model.action}}
{{/if}}

{{actionTitle}}
