<div class="group-membership">
  {{#if canManageGroup}}
  <div class="clearfix">
    {{user-selector usernames=add_usernames tabindex="1"}}
    <button {{action "addMembers"}} {{bind-attr disabled="disableSave"}} class='btn' tabindex="2">{{i18n 'admin.groups.selector_placeholder'}}</button>
  </div>
  {{/if}}

  <table class='group-members'>
    <thead>
      <tr>
        <th colspan=3></th>
        <th class="seen">
          {{i18n 'last_seen'}}
        </th>
      </tr>
    </thead>
    <tbody>
      {{#each m in model itemController="group/member"}}
      <tr class='group-member'>
        <td class='avatar'>
          {{avatar m imageSize="medium"}}
        </td>
        <td>
          <span class='username'>{{#link-to 'user' m}}{{unbound m.username}}{{/link-to}}</span>
        </td>
        <td>
          <span class='fullname'>{{unbound m.name}}</span>
        </td>
        <td>
          <span class='last-seen-at'>{{bound-date m.last_seen_at}}</span>
        </td>
        {{#if canManageGroup}}
          <td>
            <button {{action "removeMe"}} class='btn btn-danger fa fa-trash-o'></button>
          </td>
        {{/if}}
      </tr>
      {{/each}}
    </tbody>
  </table>
</div>
