{{render "header"}}

<div id='main-outlet'>
  {{outlet}}
  {{render "user-card"}}
</div>

{{#if showFooter}}
  {{custom-html "footer"}}
{{/if}}

{{render "modal"}}
{{render "topic-entrance"}}
{{render "composer"}}
