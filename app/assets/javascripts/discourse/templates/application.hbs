{{render "header"}}

<div id="main-outlet" class="wrap">
  <div class="container">
    {{custom-html "top"}}
    {{global-notice}}
  </div>
  {{outlet}}
  {{render "user-card"}}
</div>

{{#if showFooter}}
  {{custom-html "footer"}}
{{/if}}

{{render "modal"}}
{{render "topic-entrance"}}
{{render "composer"}}
