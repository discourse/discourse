<button
  class="btn {{if text 'btn-icon-text' 'no-text btn-icon'}}"
  aria-label="{{selectedTitle}}"
  type="button"
  tabindex="-1"
  title="{{selectedTitle}}">

  {{{icon}}}

  {{#if text}}
    <span class="d-button-label name">
      {{text}}
    </span>
  {{/if}}
</button>
