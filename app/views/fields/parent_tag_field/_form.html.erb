<div class="field-unit__label">
  <%= f.label field.attribute %>
</div>
<div class="field-unit__field">
  <%= f.select :parent_id, nested_dropdown(AnnotatorStore::Tag.where(creator_id: current_user.id).arrange(order: 'name')), include_blank: true %>
</div>
