<!DOCTYPE html>
<html lang="<%= html_lang %>" class="<%= html_classes %>">
  <%- if include_splash_screen? %>
    <!-- this includes the <head> and <body> tags with a splash screen -->
    <%= render partial: "layouts/application_with_splash" %>
  <% else %>
    <!-- this includes the default unchanged <head> and <body> tags without a splash screen -->
    <%= render partial: "layouts/application_without_splash" %>
  <%- end %>
</html>
