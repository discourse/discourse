diff --git a/lib/utils/process.js b/lib/utils/process.js
index 1234567890abcdef..abcdef1234567890 100644
--- a/lib/utils/process.js
+++ b/lib/utils/process.js
@@ -43,11 +43,14 @@ module.exports = class Process extends EventEmitter {
     kill(this.process, 'SIGKILL');
   }

-  onClose(code) {
+  onClose(code, signal) {
     if (!this.process) {
       return;
     }
-    log.warn(this.name + ' closed', code);
+    log.warn(this.name + ' closed', code, signal);
+    if (signal) {
+      this.stderr += `\n[testem] Process killed by signal: ${signal}\n`;
+    }
     this.process = null;
     this.exitCode = code;
     this.emit('processExit', code, this.stdout, this.stderr);
