en:
  importer:
    default_step_title: "Importing %{type}"
    done: "Done. Total runtime: %{runtime}"
    site_setting_log_message: "Updated by import script"
    fallback_names:
      user: "user"
      group: "group"
      category: "Category"
      badge: "Badge"
    uploads:
      existing_summary: "found %{existing} existing, %{missing} missing"
      deleting_surplus: "deleting %{count} surplus uploads"
      surplus_found: "found %{count} surplus uploads (run with delete_surplus_uploads to remove them)"
      upload_failed: "failed to create upload %{id}: %{error}"
      insert_failed: "failed to record upload %{id}: %{error}"
      fixer_missing: "removed missing upload %{id}"
      fixer_error: "could not check upload %{id}: %{error}"
      optimizer_upload_not_found: "could not find upload with sha1 %{sha1}"
      image_memory_limit: "Capping ImageMagick memory at %{mib} MiB per convert"
      interrupted: "Interrupted. The run is resumable — run the command again to continue."
      inline_not_configured: "There are uploads to import, but no files database is configured and inline upload mode is not set up. Either run `disco upload` to build a files database, or add an `uploads:` section with `root_paths` under `config:` in importer.yml."
