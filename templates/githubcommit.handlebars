<div class="onebox">
  <a href="{{url}}">
    {{owner}}/{{repo}}
    {{sha}}
    {{branch}}
    <h2 class="host">github.com</h2>
    <img src="{{gravatar}}" />
    {{message}}{{description}}
    {{author}}
    {{time_date}}
    {{diff_stats}}
  </a>
</div>
