# frozen_string_literal: true

RSpec.describe "second leased root" do
  before(:context) { record_scheduler_event("before-second") }
  after(:context) { record_scheduler_event("after-second") }

  it "runs the second example", :selected do
    expect(ENV["SCHEDULER_FIXTURE_EXAMPLE_FAILURE"]).not_to eq("1")
  end
end
