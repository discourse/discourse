en:
  discourse_workflows:
    errors:
      approval_timed_out: "Approval timed out"
      invalid_cron_expression: "Cron expression must be a valid 5-field cron expression."
      not_enabled: "The discourse-workflows plugin is not enabled."
      invalid_credential_type: "Unknown credential type"
      create_post:
        topic_closed_or_archived: "Cannot create a post in a closed or archived topic."
    wait:
      timer_expired: "Wait timer expired"
      webhook_method_mismatch: "HTTP method does not match configured method"
    data_table:
      not_found: "Data table not found"
      filters_required: "At least one filter condition is required"
  site_settings:
    discourse_workflows_enabled: "Enable the discourse-workflows plugin."
