<td>
  {{#if socialStyle}}
    {{topic-link topic}}
    {{raw "list/topic-intro" topic=topic}}
    {{#if showThumbnail}}
      {{raw "list/topic-thumbnail" topic=topic thumbnails=thumbnails}}
    {{/if}}
  {{else}}
    {{#if showThumbnail}}
      {{raw "list/topic-thumbnail" topic=topic thumbnails=thumbnails}}
    {{else}}
      <div class='pull-left'>
        <a href="{{topic.lastPostUrl}}">{{avatar topic.lastPoster imageSize="large"}}</a>
      </div>
    {{/if}}
  {{/if}}

  <div class='topic-details'>
    {{#if socialStyle}}
      {{raw "topic-status" topic=topic}}
      {{#if topic.unseen}}
        <span class="badge-notification new-topic"></span>
      {{/if}}
    {{else}}
      <div class='main-link'>
        {{raw "list/topic-list-title" topic=topic showTopicPostBadges=showTopicPostBadges mobileView=site.mobileView}}
      </div>
    {{/if}}

    {{#unless socialStyle}}
      <div class='pull-right'>
        {{raw "list/post-count-or-badges" topic=topic postBadgesEnabled=showTopicPostBadges}}
      </div>
    {{/unless}}

    {{#if expandPinned}}
      {{raw "list/topic-excerpt" topic=topic}}
    {{/if}}

    {{#unless socialStyle}}
      {{#unless hideCategory}}
        {{raw "list/topic-category" category=topic.category}}
      {{/unless}}
      {{#if topic.tags}}
        <div class='discourse-tags'>
        {{#each topic.visibleListTags as |tag|}}
          {{discourse-tag tag}}
        {{/each}}
        </div>
      {{/if}}
    {{/unless}}

    {{raw-plugin-outlet name="topic-list-tags"}}

    {{#if showActions}}
      {{raw "list/topic-actions" likeCountDisplay=likeCountDisplay topicActions=topicActions}}
    {{/if}}

    {{#unless socialStyle}}
      <div class='num activity last'>
        <span class="age activity" title="{{topic.bumpedAtTitle}}"><a href="{{topic.lastPostUrl}}">{{format-date topic.bumpedAt format="tiny" noTitle="true"}}</a></span>
      </div>
    {{/unless}}
  </div>
</td>
