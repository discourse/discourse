# Phosphor Fill icons used by the visual editor.
#
# One name per line, using the canonical Phosphor name (no `-fill` suffix).
# Find names at https://phosphoricons.com/ — set the variant to Fill.
#
# After editing this file, regenerate the sprite:
#   bin/rake visual_editor:phosphor_sprite
#
# In development, the sprite is also regenerated automatically at boot
# whenever this file is newer than `phosphor.svg` next to it.
#
# Each entry registers an icon at `ve-<name>` (the `ve-` prefix is added
# automatically in plugin.rb).

align-bottom
align-center-horizontal
align-center-vertical
align-left
align-right
align-top
arrows-horizontal
arrows-vertical

